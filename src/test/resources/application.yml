settings:
  app_version: 1.0
  greet_message: Hello all
  custom_message: custom all

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    platform: integrationtest
    url: jdbc:h2:mem:testdb
    username: abc
    password: abc
  h2:
    console:
      enabled: true
      path: /h2